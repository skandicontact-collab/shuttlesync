Here is **the exact correct architecture** for **ShuttleSync Destination** (TorGad/TorGas system) — based on how real airport & destination operations platforms (ACUS, AODB, DCS extensions, TUI/DER Tour, Sunclass Ops) are structured.

I’ll give you:

1. **Should it be 1 mega-page or multiple?**
2. **Correct GitHub architecture**
3. **Correct Wix architecture**
4. **How to load sub-modules without creating 100 Wix pages**
5. **Folder structure you should use**
6. **Exact pattern for scalability**

---

# ✅ 1 — **Should ShuttleSync Destination be ONE MEGA HTML?**

❌ **No.**
Do NOT put everything into one ultra-huge page.
That becomes too heavy and impossible to maintain.

❌ Do NOT create 40 Wix pages.
❌ Do NOT create one single giant HTML file.

---

# ✅ 2 — **Correct Architecture (Best Practice)**

✔ **ONE Wix page**
✔ **ONE iframe** inside that page
✔ The iframe loads *one module at a time*
✔ Each module is its **own HTML file on GitHub**

This is how airline operations systems do it (modular micro-apps).

---

# 🟩 3 — **WIX STRUCTURE (DO THIS!)**

### **Wix Page: `/shuttlesync/destination`**

Inside the page:

```
[Container]
    └── [iframe id="destinationFrame"]
```

That’s it.

Then inside Wix page code:

```js
$w('#destinationFrame').src = "DEFAULT_PAGE_URL";  // e.g. torGadHome.html
```

Every time the user clicks a feature inside the UI:

```js
$w('#destinationFrame').src = "URL_TO_NEW_MODULE";
```

Wix never changes pages.
Only the iframe content switches.

---

# 🟦 4 — **GitHub Structure (FINAL, CORRECT)**

Inside `/destination/` folder:

```
/destination
    /home
        index.html
        home.js
        home.css

    /airport
        paxList.html
        flightList.html
        hotelDistribution.html
        transferList.html
        delayMessaging.html
        misconnectAlerts.html
        gateMonitor.html
        baggageStatus.html

    /paxCI
        welcomeMeetings.html
        hotelCheckIn.html
        qrScan.html
        notesFlags.html

    /transfers
        busAssignments.html
        timetables.html
        driverSync.html
        paxCounts.html

    /tours
        productList.html
        paymentScanner.html
        bookings.html
        commission.html
        etickets.html

    /guestService
        hotlineChat.html
        issueReports.html
        lostFound.html
        accessibility.html
        emergencies.html

    /hotelRelations
        hotelDirectory.html
        roomingList.html
        complaints.html
        meetingMinutes.html

    /managerPanel
        staffRotations.html
        toursInventory.html
        dutySchedule.html
        salesReporting.html
        broadcastMessages.html

    /common
        destinationSidebar.html
        destination.css
        sidebar.js
```

Each module is its own HTML micro-app.
You can build them one at a time.

---

# 🟥 5 — **How ShuttleSync Destination UI Works**

Each category (“MY WORK”, “AIRPORT”, “TRANSFERS”, etc.) is just a **menu**.

### Menu → loads new HTML file into iframe.

Example (in sidebar.js):

```js
function loadModule(module) {
    window.parent.postMessage({
        type: "loadDestinationModule",
        url: module
    }, "*");
}
```

Then in Wix page code:

```js
window.addEventListener("message", (event) => {
    if(event.data.type === "loadDestinationModule") {
        $w('#destinationFrame').src = event.data.url;
    }
});
```

---

# 🟧 6 — **Your List Converted Into the Proper Module Layout**

Here’s your entire list reorganized as the **correct folder structure** you can copy/paste into GitHub:

---

## **✔ /destination/home/**

* checkInOut.html
* internalMail.html
* groupTalk.html
* reporting.html
* settings.html
* logout.html
* customerChat.html

---

## **✔ /destination/airport/**

* paxList.html
* flightList.html
* hotelDistribution.html
* transferList.html
* delayMessaging.html
* misconnectAlerts.html
* gateMonitor.html
* baggageStatus.html

---

## **✔ /destination/paxCI/**

* welcomeMeetings.html
* hotelCheckInList.html
* qrScan.html
* notesFlags.html

---

## **✔ /destination/transfers/**

* busAssignments.html
* timetables.html
* driverSync.html
* paxCounts.html

---

## **✔ /destination/tours/**

* productList.html
* paymentScanner.html
* bookings.html
* commissionOverview.html
* eTickets.html

---

## **✔ /destination/guestService/**

* hotlineChat.html
* issueReports.html
* lostFound.html
* accessibility.html
* emergencies.html

---

## **✔ /destination/hotelRelations/**

* hotelDirectory.html
* roomingList.html
* complaintsLog.html
* meetingMinutes.html

---

## **✔ /destination/managerPanel/**

*(Visible only to supervisors/managers)*

* staffRotations.html
* toursInventory.html
* dutySchedule.html
* salesReporting.html
* broadcastMessages.html

---

# 🟨 7 — Should /destination be ONE HTML or MANY?

### Final answer:

### **☑ ONE Wix page**

### **☑ MANY GitHub pages inside `/destination` folder**

### **☑ Load modules dynamically into one iframe**

This gives you:

✔ Fastest loading
✔ Clean architecture
✔ Easy updates
✔ No Wix clutter
✔ ACUS/DCS-style micro-app architecture
✔ Perfect with sessionManager.js auth
✔ Scalable for new tools

---

# If you want I can generate:

✔ The entire **GitHub folder skeleton** ready to paste
✔ The **sidebar menu system**
✔ The **destination home page** (with icons, categories, navigation)
✔ The **iframe switching system**
✔ The **session check code** for each module
✔ The **Wix ready embed code**

Just say:

**“Generate the folder skeleton for /destination now.”**
